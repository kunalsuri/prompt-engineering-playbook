# Copy this file to .env and fill in at least ONE API key below.
# The labs auto-detect which provider you're using.

# ─── Option 1 (RECOMMENDED — FREE) ─── Google Gemini ───
# Get a free key at: https://aistudio.google.com/apikey
GOOGLE_API_KEY=your-google-api-key-here

# ─── Option 2 (FREE) ─── Groq ───
# Get a free key at: https://console.groq.com
# GROQ_API_KEY=your-groq-api-key-here

# ─── Option 3 (PAID) ─── OpenAI ───
# OPENAI_API_KEY=sk-your-key-here

# ─── Optional overrides ──────────────────
# Override the API base URL (for Ollama, vLLM, LM Studio, etc.)
# OPENAI_API_BASE=http://localhost:11434/v1

# Override the default model (auto-detected per provider)
# LLM_MODEL=gemini-2.0-flash
# LLM_MODEL=gpt-4o-mini
